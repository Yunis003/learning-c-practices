#include "main.h"
/**
 * _isupper - function or entry point
 * @c: character that will be checked
 * Return: 0 or 1 depends on the case
 **/
int _isdigit(int c)
{
	if (c >= '1' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
